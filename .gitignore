node_modules
dist
dist-ssr
*.local
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.vscode/*
!.vscode/extensions.json
# Local Claude Code settings (user-specific)
.claude/settings.local.json
# Reference Streamlit app — not part of the React build
Risk Dashboard Final/

# Supabase CLI local state (regenerated on link)
supabase/.temp/

# Secrets — never commit real credentials. Research lives in a separate private
# repo; nothing under a research/ path belongs here any more.
.env
.env.*
!.env.example
research/
