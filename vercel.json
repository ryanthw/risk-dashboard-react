{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':(exclude)research/'",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
